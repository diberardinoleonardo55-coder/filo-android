<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Rete: solo verso il PC di casa, in HTTPS con certificato appuntato.
         ACCESS_NETWORK_STATE serve a calcolare l'indirizzo di broadcast della
         rete Wi-Fi per la ricerca del PC. -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!-- Il collegamento col PC vive in un servizio in primo piano: e' l'unico
         modo perche' regga mentre l'app non e' aperta. Da Android 14 il tipo
         va dichiarato anche come permesso. -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <!-- Fino ad Android 9 i file si scrivevano a mano nella memoria; da 10 in
         poi ci pensa MediaStore e questo permesso non esiste piu'. -->
    <uses-permission
        android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="28" />

    <application
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Filo">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- La voce "Filo" dentro il menu Condividi di qualunque app. -->
        <activity
            android:name=".CondividiActivity"
            android:exported="true"
            android:excludeFromRecents="true"
            android:label="@string/manda_al_pc"
            android:noHistory="true"
            android:theme="@style/Theme.Filo.Invisibile">
            <intent-filter>
                <action android:name="android.intent.action.SEND" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:mimeType="*/*" />
            </intent-filter>
            <intent-filter>
                <action android:name="android.intent.action.SEND_MULTIPLE" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:mimeType="*/*" />
            </intent-filter>
        </activity>

        <!-- Il ponte per gli appunti. Non ha faccia: si apre, prende il fuoco,
             legge o scrive gli appunti e si chiude. Senza fuoco Android non
             lascia toccare gli appunti a nessuno (da Android 10). -->
        <activity
            android:name=".AppuntiActivity"
            android:exported="false"
            android:excludeFromRecents="true"
            android:launchMode="singleTop"
            android:noHistory="true"
            android:taskAffinity=""
            android:theme="@style/Theme.Filo.Invisibile" />

        <service
            android:name=".PonteService"
            android:exported="false"
            android:foregroundServiceType="dataSync" />

        <!-- Il riquadro nelle impostazioni rapide: un tocco e gli appunti sono
             sul PC. exported="true" e' obbligatorio, ad aprirlo e' il sistema. -->
        <service
            android:name=".Riquadro"
            android:exported="true"
            android:icon="@drawable/ic_filo"
            android:label="@string/manda_al_pc"
            android:permission="android.permission.BIND_QUICK_SETTINGS_TILE">
            <intent-filter>
                <action android:name="android.service.quicksettings.action.QS_TILE" />
            </intent-filter>
        </service>

        <receiver
            android:name=".AvvioReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>
    </application>
</manifest>
